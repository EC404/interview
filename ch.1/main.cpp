#include "rotate_string.h"
#include "linked_list.h"

int main(int argc, char **argv)
{
    using std::cout;
    using std::endl;
    using std::string;

    LinkedList list;

    list.push(1);
    list.push(2);
    list.push(3);
    list.push(4);
    list.push(5);
    list.push(6);

    list.printList();
}